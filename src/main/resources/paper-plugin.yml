name: uHomes
version: '${project.version}'
main: me.usainsrht.uhomes.UHomes
api-version: '1.20'
authors:
  - UsainSrht
dependencies:
  bootstrap:
    Lands:
      load: BEFORE
      required: false
      join-classpath: true
    GriefPrevention:
      load: BEFORE
      required: false
      join-classpath: true